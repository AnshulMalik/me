import React from 'react';
import ReactDOM from 'react-dom';
import Container from './src/components';

//let's see if this counts
ReactDOM.render(
  <Container />,
  document.getElementById('root')
);
